node_modules/
build/
dist/
coverage/
.DS_Store
*.log
logs/
.idea/
.env*
!.env.example
.janus_ledger.json
.janus_ledger_test.json
.janus_ledger_journeys_test.json
.janus_settings.json
.janus_history.json
.janus_scrollback_*.log
.janus_interaction_log*.jsonl
.janus.db*
.janus*.bak

# wsm-e2e-pinned-arkr: raw Gemini Live capture traces (JANUS_CAPTURE_LIVE_TRACES=1). UNREDACTED —
# operator/model speech + tool args verbatim. Never commit directly; run scripts/redact-trace.mjs
# and commit only the reviewed *.redacted.jsonl sibling (see docs/process/LIVE_TRACE_CAPTURE.md).
.janus_traces/
_ptyprobe_test*.cjs
dist-tmp/

# Python bytecode
__pycache__/
*.pyc
python/**/__pycache__/

# Playwright e2e
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
e2e-artifacts/

# Generated by tests/test_exchange_benchmarks.ts (Phase 5, Step 5.3 release benchmark harness) —
# a machine-readable report artifact, not source. Override the path with --report/EXCHANGE_BENCHMARK_REPORT.
/benchmark-report.json
# Pixel-parity baselines are a Windows-local before/after proof artifact (win32-specific, CI-skipped),
# not a committed cross-platform golden — see e2e/ledger_data_pixel_parity.spec.ts.
e2e/*-snapshots/

# Claude Code machine-local settings (not committed)
.claude/settings.local.json

# Superpowers brainstorm/visual-companion artifacts (local only)
.superpowers/

# git worktree directories (never commit a worktree's nested .git pointer)
.claude/worktrees/

# beads issue tracker — local Dolt DB; sync is via refs/dolt/data, not the working tree
.beads/
